A company has three computers connected to a private network. The company has been allocated only one public IP address by the ISP.
Configure PAT (Port Address Translation) on the router so that all three computers can access an external server simultaneously using the single public IP address.

NETWORK TOPOLOGY
          Server
     203.0.113.100
           |
     G0/1 203.0.113.2
        Router
     G0/0 192.168.1.1
           |
        Switch
      /    |     \
    PC0   PC1   PC2

